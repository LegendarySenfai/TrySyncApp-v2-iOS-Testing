# Copy this file to .env and fill in real values.
# .env itself is gitignored — never commit real credentials.

# --- Database (Aiven MySQL) ---
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=Senfai_123!
DB_NAME=trisyncdb

# --- Auth ---
# Generate a strong random value, e.g.: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=32bb404be9bf0e1668c544801028349d293099b074fc101d04d4c2a41a71a753b22269626802e971d7ca75d118be350e

# --- Email (Gmail SMTP — used for OTPs and AI alert emails) ---
# EMAIL_PASS must be a Gmail "App Password", not your normal account password.
EMAIL_USER=skustateethclinic@gmail.com
EMAIL_PASS=pavd oqpp buto ylnl

# --- Frontend URL (used in outgoing email links) ---
FRONTEND_URL=https://duo-sync-app.vercel.app

# --- AI ---
GEMINI_API_KEY="AIzaSyAsC7YDUWM2u_uHHC7i43ZU5SH34MXHT30"

# --- Server ---
PORT=5000
